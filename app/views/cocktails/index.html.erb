<div class="container">

  <div class="cocktails">
    
    <% @cocktails.each do |cocktail| %>
        <div class="cocktail-card">
          <%= link_to cocktail.name, cocktail_path(cocktail) %>
        </div>
    <% end %>
  </div>
</div>
